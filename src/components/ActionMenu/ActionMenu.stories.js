import React from 'react';
import { action } from '@storybook/addon-actions';

import ActionMenu from './ActionMenu';

export default {
	component: ActionMenu,
	title: 'ActionMenu',
};

export const icon = () => <ActionMenu />;